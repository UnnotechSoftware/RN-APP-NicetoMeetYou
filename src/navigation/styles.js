import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    backgroundColor: "#edcf64",
  },
  headerTitleStyle: {
    fontSize: "24px",
    marginBottom: "5px",
  },
});
